[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback for React Router deep links (belt and braces with public/_redirects).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
